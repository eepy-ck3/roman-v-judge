# Roman vs Judge Bet Tracker — Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.115.0          # Modern async web framework
uvicorn[standard]==0.32.0  # ASGI server (websockets + httptools for speed)

# ── HTTP Client ────────────────────────────────────────────────────────────────
requests==2.32.3           # Sync HTTP for API calls (simpler than httpx for our use case)

# ── Templating ─────────────────────────────────────────────────────────────────
jinja2==3.1.4              # HTML email template rendering

# ── Scheduling ─────────────────────────────────────────────────────────────────
apscheduler==3.10.4        # Monday morning cron job

# ── Config ─────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1       # Load .env file into environment

# ── SMS ────────────────────────────────────────────────────────────────────────
twilio==9.3.3              # SMS via Twilio API

# ── Data Validation ────────────────────────────────────────────────────────────
pydantic==2.9.2            # FastAPI depends on this; used for request models

# ── Static Files ───────────────────────────────────────────────────────────────
aiofiles==24.1.0           # FastAPI static file serving

# ── AI Narrative (weekly email summary) ────────────────────────────────────────
anthropic>=0.40.0          # Claude API — Jeff Passan-style weekly recap

# ── Optional but nice ──────────────────────────────────────────────────────────
# python-multipart==0.0.12  # Uncomment if you add file upload endpoints
